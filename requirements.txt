# Music Theory Master - runtime + build dependencies
# Pinned to versions verified on this machine (Python 3.14, win64).
PyQt6==6.10.2
music21==9.9.1
numpy==2.3.5
scipy==1.17.0
sounddevice==0.5.5
pygame-ce==2.5.6
pyfluidsynth==1.3.4
mido==1.3.3
defusedxml==0.7.1

# Build-time only (packaging the .exe). Safe to install for dev too.
pyinstaller==6.16.0
